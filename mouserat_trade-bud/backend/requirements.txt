# mouserat_trade-bud backend -- local FastAPI dev server (own tracked list,
# mirrors discord_bot/requirements.txt's pattern: this subsystem has its own
# runtime lifecycle, separate from the ETL/notebook .venv's requirements.txt).
# Install into the repo's .venv: .venv\Scripts\python.exe -m pip install -r mouserat_trade-bud\backend\requirements.txt

fastapi>=0.115
uvicorn>=0.30
httpx>=0.27        # transitively required by discord_bot/github_fetch.py at import time
python-dotenv>=1.0 # ditto, via discord_bot/config.py (imported by capmath.py)
pandas>=2.0
pyarrow>=15
numpy>=1.26         # directly imported by pick_value.py's quantile mapping

# NOTE: this file is what CI installs to build the static site, so it must
# cover the *whole* import chain, including everything data_access.py pulls in
# from discord_bot/. The local .venv installs the root requirements.txt too, so
# a gap here runs fine locally and only fails in CI -- as python-dotenv did.
# Test changes in a clean environment, not against the .venv.
